# Acme wire field schema (fixture)

Process-quality fixture for multi-step fix-loop evals. Vendor id `acme` is generic/fake.

## Purchase payload fields

Domain `eventId` maps to wire field **`event_id`**.

Required wire field: `event_id` (maps domain eventId).

Do **not** use legacy `evt_id` (removed from public schema).
