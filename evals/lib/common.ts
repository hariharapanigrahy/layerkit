/**
 * @deprecated Prefer evals/harness/assert.js — re-export kept for older imports.
 */
export {
  pass,
  fail,
  assertTrue,
  assertEqual,
  assertThrows,
  assert,
} from '../harness/assert.js';
