---
name: layerkit-update-maps
description: When vendor docs change, refresh maps via new proposals (working-memory update analog).
---

# layerkit-update-maps

1. Diff current map vs latest docs
2. New proposal with sources + changelog summary
3. validate → human approve → apply
4. Re-run Java skill if wire shape changed
