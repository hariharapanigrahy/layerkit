---
name: layerkit-generate-java
description: Agent implements enterprise Java client from applied maps (CLI only scaffolds Maven).
---

# layerkit-generate-java

```bash
layerkit generate --lang java
```

Read `.layerkit/out/java/AGENT_TASK.md`. Implement `DataLayerClient.track` for filled vendors only. No LLM on hot path.
