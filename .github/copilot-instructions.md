# Copilot Instructions

Read and follow `AI_WORKING_RULES.md`.

Prefer update/delete over adding new files. Before implementation, define what must pass, the proof artifact, and the fallback if the design fails. Prove strategic redirects before broad rewrites or deletions. Keep tests proportional to implementation size. Treat hardcoded secrets as release blockers in public/shared repos.
